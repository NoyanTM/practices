services:
  keycloak:
    image: quay.io/keycloak/keycloak:26.3.5
    container_name: keycloack_practice-4
    env_file: .env
    # TODO: environment variables
    ports:
      - "127.0.0.1:8080:8080"
    command: start-dev
